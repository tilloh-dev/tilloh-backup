# AGENTS.md

## Repository purpose

A personal tooling collection: curated developer tool references (`README.md`), bash config backups, OpenCode config backup, Claude config backup, and a fresh-Linux bootstrap guide.

No build system, no tests, no CI. All content is documentation and shell scripts.

## Structure

```
tooling/
├── README.md                        # Curated tool/service link list
├── .opencode/skills/                # Project-local skills (OpenCode reads these)
│   └── llama-preset/                # models.ini section generator + recommend.sh
├── bashrc-backup/                   # Bash config to deploy to ~
│   ├── .bashrc                      # PS1 + sources aliases/functions
│   ├── .bashrc-aliases              # All shell aliases
│   ├── .bashrc-functions            # All shell functions
│   └── install.bash                 # Deploys to ~; replaces CUSTOM block
├── opencode-backup/
│   ├── opencode.jsonc               # OpenCode global config
│   ├── AGENTS.md                    # Global personal rules -> ~/.config/opencode/AGENTS.md
│   ├── skills/                      # OpenCode skills, incl. commit-push (agents/ too if present)
│   └── install.sh                   # Copies config + AGENTS.md + skills/ to ~/.config/opencode/
├── claude-backup/
│   ├── .claude/                     # settings.json, mcp.json, commands/, skills/
│   └── install.sh                   # Copies to ~/.claude/
├── llama.cpp/                       # Local LLM inference (prebuilt Vulkan llama.cpp)
│   ├── bootstrap.sh                 # Fetch+extract prebuilt Vulkan release into vendor/
│   ├── download-model.sh            # Pull GGUF(s) from HuggingFace to $LLAMA_MODELS_DIR
│   ├── server.sh                    # Start llama-server (router or single model)
│   ├── config-load.sh               # Sourced by all three: env > config.env > config.ps1
│   ├── config.env.example           # Paths/version/port (copy to config.env)
│   ├── models.list                  # HuggingFace download manifest
│   ├── presets/models.example.ini   # Router preset template
│   ├── PLAN.md / README.md          # Design + usage
│   └── vendor/ cache/               # Binaries + downloads (gitignored)
└── fresh_linux/debian-based/
    └── bootstrap-guide.md           # New machine setup checklist
```

## Install commands

| What | Command |
|------|---------|
| Bash aliases + functions | `cd bashrc-backup && bash install.bash` |
| OpenCode config | `cd opencode-backup && bash install.sh` |
| Claude Code config | `cd claude-backup && bash install.sh` |
| Local llama.cpp (Vulkan) | `cd llama.cpp && cp config.env.example config.env && bash bootstrap.sh` |

- `bashrc-backup/install.bash` deletes the `# CUSTOM START` … `# CUSTOM END` block from `~/.bashrc`, appends the new block at the end, then calls `exec bash -l` to reload the shell.
- `opencode-backup/install.sh` copies `opencode.jsonc` to `~/.config/opencode/opencode.jsonc`, `AGENTS.md` to `~/.config/opencode/AGENTS.md`, any `agents/*.md` to `~/.config/opencode/agents/`, and `skills/*/` to `~/.config/opencode/skills/` (creates dirs if needed). Add-only: existing agents/skills/plugins from other sources are kept. It ships **no plugin** — the bash guard comes from `stadtwerk_ai_config` (see below).
- `claude-backup/install.sh` copies `settings.json`, `settings.local.json`, `mcp.json`, `commands/`, and `skills/` into `~/.claude/`. Note: `settings.local.json` is in `.gitignore` and won't be present in a fresh clone — the script will fail on that step; ignore or create an empty file first.

## Key aliases (after install)

| Alias | Expands to |
|-------|-----------|
| `oc` | `opencode` |
| `g` / `gs` / `gp1` / `gp2` | `git` / `git status` / `git pull` / `git push` |
| `gac 'msg'` | `git add . && git commit -m 'msg'` |
| `gcob <branch>` | `git switch -c <branch>` |
| `gl` | fancy graph git log |
| `c` / `h` | `clear` / `history` |
| `k` | `kubectl` |
| `kg` / `kl` / `kp` | `kubectl get pod` / `kubectl logs` / `kubectl port-forward` |
| `kdepl` / `kstate` | `kubectl get deployment` / `kubectl get statefulset` |
| `ksvc` / `ks` / `kexs` | `kubectl get svc/secrets/externalsecrets` |
| `kpv` / `kpvc` | `kubectl get pv/pvc` |
| `kr` / `krs` | `kubectl rollout restart deployment/statefulset` |
| `goprod` / `gotest` | `kubectx` to prod / test EKS cluster |
| `gobastion` | exec into documentdb-client pod in alfresco namespace |
| `flux1` / `flux2` | reconcile flux-system source / apps kustomization |
| `awslogin` | `aws sso login --profile c4-sso` |

## Key functions (after install)

| Function | Purpose |
|----------|---------|
| `go <ns>` | Set current kubectl namespace, then print `whereami` |
| `whereami` | Show kubectx + current cluster/namespace |
| `watching <alias>` | `watch` the command behind a shell alias |
| `kex <pod>` | `kubectl exec -it <pod> -- sh` |
| `kd <type> <name>` | `kubectl describe <type> <name>` |
| `klp <pod>` | Pod logs piped through `pino-pretty@13.0.0` |
| `kwo <ns>` | Watch pods in namespace, filtering out `c4-*` |
| `serve <app> <flags>` | `nx serve` piped through `pino-pretty@13.0.0` |
| `restarting <type> <ns>` | Restart all `c4-*-backend` deployments/statefulsets in a namespace |
| `kdel <type> <name>` | Delete a k8s entity with confirmation prompt |
| `delete_with_status <ns>` | Delete pods with `ContainerStatusUnknown` in a namespace |

This table said `switch <ns>` until 2026-08-11, when a live check showed no such function exists. The namespace switcher is `go` and always was in the installed shell; only its own `check_args` usage string still prints `switch <namespace>` (`.bashrc-functions:147`), which is a leftover from the rename. Do not "correct" the function name to match that string — `go` is what is used.

## OpenCode config notes

- No global default `model` is pinned in `opencode.jsonc` (pick per session).
- `opencode-backup/AGENTS.md` → `~/.config/opencode/AGENTS.md` holds the **global personal rules** (currently: German conversation / English artefacts, the human-vs-agent writing register, measured-vs-guessed numbers, cleaning up processes and temp files you started, and verifying documented values against the live system with read-only commands). Global and project rules are separate categories in OpenCode, so this file is loaded *in addition to* a project's own `AGENTS.md`, in every session. That is deliberate: a preference that must hold for every edit belongs here and not in a skill, because skills only enter context when their `description` matches and they are known to undertrigger. Keep the file short — it costs context in every session.
- Two llama.cpp providers: `lieselotte` (local, `127.0.0.1:8081`) and `hermine` (remote, `hermine:8081`). Model IDs are bare aliases without the `.gguf` suffix.
- Both `lieselotte` and `hermine` list the **same twelve** model IDs, and both `models` maps still match their `whitelist` exactly: `Qwen3.8-27B`, `Qwen3.6-27B`, `Qwen3.6-35B-A3B`, `gemma-4-26B-A4B`, `Ornith-1.0-9B`, `Ornith-1.0-35B`, `Qwen-AgentWorld-35B-A3B`, `gemma-4-31B`, `Laguna-XS-2.1`, `Ternary-Bonsai-27B`, `Muse-Glimmer-30B`, `Nemotron-3.5-Lightning-30B-A3B`. `Qwen3.8-27B` was added from outside and re-tuned by measurement on 2026-08-15 — see its own bullet below; its GGUF + mmproj were only verified present on hermine; `Nemotron-3.5-Lightning-30B-A3B` was added 2026-08-15 and **its GGUF has only been verified present on hermine** — lieselotte's copy was not checked. **The earlier claim that only five GGUFs sit on hermine was stale and is now false** — listing the models directory on 2026-08-10 shows all ten present (gemma-4-26B-A4B and gemma-4-31B carry two files each, their MTP drafters; Muse-Glimmer carries three, drafter plus mmproj). The same bullet also claimed Ornith and Qwen-AgentWorld "will fail to load until the weights are downloaded", which contradicted the eleven-real-load Ornith-1.0-35B measurement recorded in this very file — an internal inconsistency that survived because nobody checked the disk.
- **The model ID is the `models.ini` section name, case-sensitive, and two entries were dead because of it.** `opencode.jsonc` carried `ornith-1.0-9b` / `ornith-1.0-35b` while the router serves `Ornith-1.0-9B` / `Ornith-1.0-35B`; a request with the lowercase form returns `400 model 'ornith-1.0-9b' not found`, so neither was ever selectable. Verified 2026-08-10 by diffing the whitelist against `/v1/models` and then issuing the failing request. Both are corrected. When adding a model, take the ID from `/v1/models` rather than from the `alias` key — `alias` is often the bare filename (`gemma-4-31B-it-qat-UD-Q4_K_XL.gguf`) and is *not* what clients send. `/v1/models` also lists one entry that is not ours, `unsloth/gemma-4-E4B-it-GGUF:Q4_K_XL`, from llama.cpp's own HF cache.
- **`gemma-4-26B-A4B` and `gemma-4-31B` are declared image-capable in `opencode.jsonc` but are not.** Neither preset section sets an `mmproj` key, and the only mmproj on hermine is Muse-Glimmer's (verified 2026-08-10 — a search for `*mmproj*`/`*clip*` across the models tree returns exactly one file). So `Muse-Glimmer-30B` is currently the only genuinely multimodal entry, and its `"input": ["text", "image"]` is correct while the two gemma ones will fail the moment someone pastes an image. Left as-is deliberately: the fix is either downloading the two mmproj files or narrowing the declaration, and that is a capability decision rather than a typo.
- Qwen3.8-27B (Qwen/unsloth, Apache-2.0, dense VLM, arch `qwen35`): dense 27B, 64 layers per the card (66 in the GGUF), hybrid layout `16 × (3 × Gated-DeltaNet → FFN → 1 × Gated-Attention → FFN)` — only 16 layers carry a growing KV cache (4 KV heads, head dim 256), native 262144 ctx (extensible to 1M via static YaRN, not set: the card itself warns it degrades short texts). `IQ4_XS` is 14978 MiB, plus `mmproj-F16.gguf` (927 MB) wired with `mmproj-offload = false` (host RAM) — genuinely multimodal, unlike the two gemma sections.
  **The MTP head is embedded in the main GGUF although the repo name carries no `-MTP-`** — `qwen35.nextn_predict_layers` sits in the GGUF metadata (verified 2026-08-15 by strings on the header). `recommend.sh` missed it and proposed bare `ngram-mod`: its detection keys on the HF repo name or a sibling `mtp-*.gguf`, and neither exists here. Known script gap; the measurement below is why `draft-mtp` stays.
  **MTP is a 1.85× win here — the opposite of Nemotron, because the target is dense.** Measured 2026-08-15 on hermine, build 10424, ctx 131072, KV `q8_0`/`q8_0`, `fit = off`, same short-prompt/400-token probe throughout: `draft-mtp,ngram-mod` with `spec-draft-n-max = 3` generates **78.1 / 76.5 t/s** (two runs, acceptance 0.57–0.59, mean draft length 2.71–2.77 — the budget of 3 is nearly saturated); **`spec-draft-n-max = 4` measured 62.0 t/s** (acceptance collapses to 0.347), so the inherited 3 stays and 4 is measured-worse, unlike gemma-4-26B-A4B where 4 saturates; bare `ngram-mod` gives **42.3 t/s**. A dense 27B target forward is expensive, so speculation pays — the Nemotron A3B logic inverts cleanly.
  **`ctx-size = 147456` with KV `q8_0`/`q8_0` — the final state after a same-day detour through q4_0@229376, settled by measurement on build 10437 (user decision 2026-08-15).** After the GT 610 install freed the 4090 (see the display-GPU bullet under llama.cpp config notes), the first brief was "highest context with q4_0 KV": fit-params at `q4_0`/`q4_0` reports 196608 → 18677, 229376 → 19413, 262144 → 20149 MiB (excluding MTP), and real loads gave **262144 fails** (`cudaMalloc failed: out of memory` on a 1360 MiB compute buffer), **229376 loads at 22018–22243 MiB** (~785–1010 MiB free). Then the q8_0 counter-test on the same build erased the expected throughput difference — **q8_0@131072 and q4_0@229376 both measure ~58 t/s at 26.6k depth and 60–67 t/s on short probes** (acceptance ~0.35–0.50 in 8+ runs across both) — so the decision reduced to quality+margin vs. window, and q8_0 won on the user's quality weighting. The q8_0 ladder at the freed baseline: 131072 → 21497 MiB (1531 free), **147456 → 22141–22257 MiB (771–887 free)**, 163840 → **fails** (880 MiB MTP compute buffer OOM, so the open question from the earlier baseline is now closed — it does not load even with the card fully free). 147456 was chosen over 131072 after measuring that **an image request costs only ~70 MiB of VRAM** (22185 → 22257 MiB while processing a 896×896 PNG → ~800 visual tokens): with the mmproj on the CPU the margin is static — KV and compute are preallocated — so the Muse-Glimmer request-time-allocation risk does not apply to this section and the thin margin is text-and-vision-safe. Long-prompt at 147456: 2308 t/s prompt / 58.3 t/s generation.
  **The embedded MTP context scales with ctx-size**: real-load overhead above the fit-params lower bound was ~1.85 GiB at 131072 and ~2.6 GiB at 229376, and it is what kills 262144 (q4_0) and 163840 (q8_0). **A suspected MTP regression or plain n=2 luck taints the older numbers**: on build 10424, q8_0@131072 measured 78.1 / 76.5 t/s with acceptance 0.57–0.59 (two runs); on 10437 the same configuration lands at 52–67 t/s with acceptance 0.27–0.49 (six runs), hardware ruled out (PCIe Gen4 x16, 2865 MHz SM, 327 W during generation). Nobody has re-run 10424 to separate the two explanations — do that before citing the 78 t/s anywhere. Short-prompt prompt-throughput figures (33–318 t/s) are noise; ignore them. The section pre-dating all this carried `ctx-size = 200000` at `q4_0` from the outside session. The server also prints `Qwen-VL models require at minimum 1024 image tokens … try --image-min-tokens 1024` — not set (the card does not mention it), but it is the first lever if vision grounding ever disappoints.
  Sampling: the card publishes exactly **two** whole sets — thinking (`temp 1.0`, `top-p 0.95`, `top-k 20`, `min-p 0.0`, `presence-penalty 0.0`, `repetition-penalty 1.0`) and instruct (`0.7`/`0.80`/`1.5`). **No precise-coding set exists, unlike Qwen3.6** — do not import the 0.6 from the neighbouring section. The thinking set is adopted whole; the card's own agentic benchmarks (SWE-bench Pro, DeepSWE, QwenSWEBench) all ran `temp=1.0, top_p=0.95`.
  **First section to set `reasoning-preserve = true`, and the Ornith precedent is satisfied rather than broken**: the card states "`preserve_thinking` is enabled by default for all workloads" and the embedded template was read directly — `enable_thinking is undefined or ... is true` → thinking on by default (no `reasoning = on` needed), and `{%- if preserve_thinking is undefined or preserve_thinking is true ...%}` renders thinking blocks for **all** history messages by default. The server accepted the flag with no warning. `reasoning_effort` (xhigh default) is a client-side template kwarg, not a preset key.
  `cache-reuse = 256` is inert here with a **new** message on build 10424: `cache_reuse is not supported by multimodal, it will be disabled` — the blocker named on this model is the mmproj, not the context type as in the build-10243 findings below. Key kept, consistent with everywhere else. `load-mode = dio` and `kv-unified = true` arrived with the from-outside section and are carried as unmanaged keys.
- Qwen3.6-27B (unsloth `-MTP-` build, `IQ4_XS`): the MTP speculator ships inside the GGUF, so `spec-type = draft-mtp` needs **no** `model-draft`. Presets differ per machine: **hermine** (RTX 4090, 23028 MiB, CUDA) `ctx-size = 131072`, KV `q8_0`/`q8_0`, `spec-draft-n-max = 3` — measured 2026-08-04 at 21877 MiB peak (~1.1 GiB free), 2516 t/s prompt / 97 t/s generation; **lieselotte** (7900 XTX, Vulkan) `device = Vulkan0`, `ctx-size = 262144`, KV `q8_0`/`q4_0`, `spec-draft-n-max = 4`. Sampling: the card gives two thinking-mode sets — `temp 1.0` for general tasks and `temp 0.6` for precise coding — both with `top-p 0.95`, `top-k 20`, `min-p 0.0`, `presence-penalty 0.0`; hermine's preset uses the 0.6 coding set, lieselotte still uses 1.0. hermine additionally chains a second, draft-model-free speculator: `spec-type = draft-mtp,ngram-mod` with `spec-ngram-mod-n-match/min/max = 24/48/64` (these three equal the build's defaults and are written out only to be explicit). ngram-mod is a 16 MiB host-RAM hash table (no VRAM) that hashes the last 24 tokens and chains up to 64 drafted tokens, discarding the draft entirely if the chain breaks before 48 — so it pays off on verbatim repeats (file rewrites, quoted diffs) and stays silent on novel text. Priority is **hardcoded in llama.cpp, not list order** (`common/speculative.cpp` `common_speculative_init`): all `ngram-*` implementations run before all `draft-*`, so MTP only drafts when ngram-mod returns nothing. Enabling it also raises the global draft budget to 64 via `common_speculative_n_max`, which enlarges the verification batch. Speed impact on hermine is **unmeasured** — the 97 t/s above predates it. `fit = off` is set deliberately although it is nearly a no-op here (`--fit` only adjusts arguments left unset, and this section sets `ctx-size`/`n-gpu-layers` itself); the real effect is that a VRAM shortfall now fails loudly instead of silently shrinking, which matters because the measured peak leaves only ~1.1 GiB free.
- Qwen3.6-35B-A3B (unsloth `-MTP-` build, `UD-IQ4_XS`, 18.2 GB on disk): 35B-A3B MoE (arch `qwen3_5_moe`, 40 layers, 256 experts + 1 shared, 8 routed active, native 262k ctx). Hybrid layout `10 × (3 × Gated-DeltaNet → MoE → 1 × Gated-Attention → MoE)` means only 10 of 40 layers carry a growing KV-cache (2 KV heads, head dim 256), which is why 262k ctx is affordable at `q8_0`. MTP ships inside the GGUF, so `spec-type = draft-mtp,ngram-mod` needs no `model-draft`. **Measured** on hermine with `llama-fit-params` 2026-08-04 at `ctx-size = 262144` + `q8_0`/`q8_0` + `flash-attn on`: model 16435 + KV 2782 + compute 820 = **20037 MiB of 23027** (~2990 MiB free), plus 515 MiB model / 264 MiB compute on the host. That figure **excludes the MTP context** — `llama-fit-params` rejects `--spec-type`, so the real peak is higher and remains unmeasured; the ~2.9 GiB headroom is the reason 262144 is kept anyway. `cache-type-k/v-draft = q4_0` is set, but note it was added on the assumption that it saves VRAM, and the gemma-4-31B measurement below disproves that for MTP drafters generally — it is kept because it is free, not because it was measured to help here. Sampling: the card gives three **whole** sets and they are not to be mixed — thinking/general `temp 1.0` + `presence-penalty 1.5`, thinking/precise-coding `temp 0.6` + `presence-penalty 0.0`, instruct `temp 0.7` + `presence-penalty 1.5` (all three share `top-p` 0.95/0.95/0.80, `top-k 20`, `min-p 0.0`). The presets use the coding set whole. Pulling only the `1.5` into it would be a combination the vendor never publishes, and it is wrong on the merits: a presence penalty punishes repeated tokens, which is precisely what code consists of, and the card warns that higher values can cause language mixing. The one place the two models differ is the *general* set — 35B uses `presence-penalty 1.5` there, 27B uses `0.0` — so that difference is invisible as long as both run the coding set. The presets use the coding set — before this change both `models.ini` and `models.example.ini` set **no sampling at all** for this model and silently ran llama.cpp's defaults (`temp 0.8` / `top-p 0.9` / `top-k 40`), which contradicts the card. It is also a VLM, but no `mmproj-*.gguf` is present in the model directory, so the presets are text-only.
- Ornith-1.0 (DeepReinforce/ornith-ai, MIT, agentic coding): post-trained on Gemma 4 and Qwen 3.5, so the 9B reports arch `qwen35` — which is why its sampling looks like Qwen's. It is not copied: the card recommends `temp 0.6`, `top-p 0.95`, `top-k 20` verbatim (and `temp 1.0` only to reproduce the published benchmarks), so the values already in both presets are vendor-correct. A reasoning model — the assistant turn opens with `<think>…</think>` by default, and the vendor's serving recipes add a reasoning parser plus a `qwen3_xml`/`qwen3_coder` tool-call parser, which `jinja = true` + `reasoning-format = auto` cover. Unlike Laguna the embedded template does **not** default thinking off, so no explicit `reasoning = on`. **9B measured on hermine 2026-08-05** by loading at the full native `ctx-size = 262144`, KV `q8_0`/`q8_0`: **14958–14977 MiB of 23028** (~8060 MiB free), **9247 t/s prompt** (7230-token prompt), 75.3 t/s generation on a short prompt and 73.7 t/s after the long one. No MTP drafter exists for it, so `llama-fit-params` is complete here rather than a lower bound — and its estimate (13783 MiB) came within ~1.2 GiB of the real load. The 8 GiB left idle cannot be turned into context — 262144 is already `n_ctx_train`. It could *almost* be turned into a better quant, and an earlier version of this paragraph got that wrong by mixing decimal GB with GiB: BF16 is 17.9 GB **decimal** = 17071 MiB, and 17071 + the measured 5872 MiB of KV/compute = 22943 of 23028, so it would nominally fit with 85 MiB to spare rather than "not fit". The reason to stay on Q8_0 is throughput, not capacity. Generation here is memory-bandwidth-bound: 75.3 t/s × 9.53 GB of weights ≈ 718 GB/s effective against the 4090's ~1008 GB/s spec, so doubling the weight bytes would land near **40 t/s**. That number is **computed from the measured one, not measured** — nobody has loaded a BF16 build of this model on this box. Two runs bracket what `ngram-mod` is worth on novel text — 75.30 t/s with it against 75.32 t/s without, i.e. nothing, exactly as the mechanism predicts. `cache-type-v` was `q4_0` (lieselotte's Vulkan floor) and is now matched to K on hermine.
- Ornith-1.0-35B on hermine is the section that **disproved this repo's own ctx policy**, measured 2026-08-05 with eleven real loads. `Q4_K_M` is 20186 MiB on a 23028 MiB card, so the inherited `ctx-size = 262144` could not have loaded at all. With `fit = off` (full GPU residency) generation is **flat** at 158.5 / 157.9 / 157.4 t/s for ctx 32768 / 65536 / 98304 and prompt sits near 6600 t/s — context is free in that range. At 131072 it collapses to 87.0 t/s and 3728 t/s prompt, for **176 MiB** less free VRAM (701 → 525), and 163840 refuses to load. The cliff is reproducible but its **cause is not established**, and the obvious explanation is already dead: "the Windows driver spills to shared memory once VRAM is nearly full" was tested against gemma-4-31B on 2026-08-05 and fails — that model runs 77.3 / 78.2 / 78.1 t/s at 577 / 1540 / 2508 MiB free, i.e. flat, with no CPU offload, at a margin *tighter* than the 701 MiB where Ornith is still fine. So free VRAM alone does not predict it; something about this model's allocation at 131072 does, and nobody has looked with a driver counter. The counter-intuitive consequence: with `fit = on` llama.cpp silently pushes MoE experts to CPU (`tensor overrides to CPU are used with mmap enabled`) and reaches 110.9 t/s at 131072 — *faster* than `fit = off` at the same ctx (87.0), because dodging the cliff beats falling off it. But both lose badly to simply staying below it, so the skill's rule "shrink ctx rather than offload experts" survives, while its unstated assumption "full GPU residency is always fastest" does not. Chosen `ctx-size = 65536` (1111–1137 MiB free, 153.0–157.9 t/s across two runs) over 98304, which measured equally fast (157.4 t/s) but is the *last* known-good point before the collapse; 65536 is one 32k step below it and was measured twice. That is the whole argument — the original "leaves only 701 MiB free" reasoning does not survive the gemma-4-31B result above. `fit = off` is **load-bearing here, not cosmetic** as it is on Qwen3.6-27B: without it this section silently runs the slow offloaded configuration. Two things the server itself reported and that are **not** acted on: `chat template supports preserving reasoning, consider enabling it via --reasoning-preserve` — the card documents `reasoning_content` but never says thinking should be carried across turns, and Qwen 3.5, which this is post-trained on, says the opposite, so the skill's "do not set what the card does not confirm" applies. The suspicion that **gemma-4-31B sits in this same cliff was tested and is false** — see its own bullet above; its 65536 stays.
- `cache-reuse` is **inert on this llama.cpp build (10243) — everywhere, not per model.** Every load logs `cache_reuse is not supported by this context, it will be disabled`. Measured 2026-08-05 across four architectures (gemma4 dense 31B, gemma4 MoE 26B-A4B, qwen35 dense 9B, Qwen3.6-27B) and, on the 9B, across five flag combinations: with speculative decoding, without it, `-fa off`, KV `f16` instead of `q8_0`, and `--kv-unified`. All disabled. So it is neither the hybrid-attention architecture nor speculative decoding nor FlashAttention, as was assumed twice before this was tested properly. The key is still written into every section because it costs nothing and would start working on a build that supports it — but do not credit it with anything, and do not let the llama-preset skill's description of it ("reuses cached KV for repeated prefixes across turns") be read as a statement about this machine.
- Laguna XS 2.1 (poolside, OpenMDW-1.1, agentic coding): 33B-A3B MoE (arch `laguna`, 40 layers, 256 experts + 1 shared, native 262k ctx). Mixed attention layout — 10 global-attention layers + 30 sliding-window (512) layers in a 3:1 ratio with sigmoid gating and per-layer rotary scales — so only a quarter of the layers carry a growing KV-cache and long contexts stay cheap on VRAM. Built for long-horizon agent work (SWE-bench Verified 70.9, SWE-bench Multilingual 63.1, Terminal-Bench 2.0 37.5). Reasoning is interleaved between tool calls, and the model expects *preserved thinking* (prior assistant `reasoning_content` kept in the message history) → `jinja = true` + `reasoning-format = auto`, and clients that drop thinking blocks will see it stop reasoning in follow-up steps. **Thinking is not on by default in this GGUF**, contrary to what the card implies: the embedded template opens with `{%- set enable_thinking = enable_thinking | default(false) -%}` (verified 2026-08-04 by reading the template out of the GGUF metadata — note it sits past the 100 MB mark, a `strings` window of a few MB misses it entirely). Only under `enable_thinking` does the template render `<think>` + `reasoning_content` + `</think>` for *every* history message, which is exactly the preserved thinking the agent loop needs; the else branch emits a bare `</think>`. hermine's preset therefore sets `reasoning = on` explicitly rather than trusting `--reasoning auto` to flip it. Whether `auto` would have flipped it anyway is **unverified** — the template default is the measured fact, the behaviour of `auto` against this template is not. If a rendered prompt ever shows thinking still off, `chat-template-kwargs = {"enable_thinking": true}` sets the template variable directly. Whether the template declares `supports_preserve_reasoning` (what `--reasoning-preserve` looks for) is also unverified: the string is absent from a `strings` dump, but that dump was not a structured metadata read. Upstream llama.cpp support merged via PR #25165 (2026-07-22). Preset uses bartowski's imatrix `IQ4_XS` (18.2 GB) for KV/compute headroom on a 24 GB card. hermine runs `ctx-size = 163840` with KV `q8_0`/`q8_0` — **measured** with `llama-fit-params` 2026-08-04: model 17259 + KV 3463 + compute 837 = 21559 MiB of 23027 (~1468 free). The neighbouring points were measured too: 131072 → 20719 (2308 free), 196608 → 22399 (628 free), 262144 → 24079, which does **not** fit. 163840 was chosen over 196608 because 628 MiB is as thin as the gemma-4-31B section that this file already flags as a low-margin gamble. Unlike the Qwen sections these figures are complete rather than a lower bound, because Laguna has no MTP drafter and `ngram-mod` needs no context of its own. The previous `ctx-size = 32768` was pure caution and cost a factor of five. Its KV pair was also the mixed `q8_0`/`q4_0` that collapses throughput on this CUDA box (see the note further down), so both were wrong at once. Vendor sampling (the only preset the card documents, used for all published benchmarks): `temp 1.0`, `top-p 1.0`, `top-k 20`. A DFlash speculator exists (`poolside/Laguna-XS-2.1-DFlash`) and the build advertises `--spec-type draft-dflash`, but no GGUF in llama.cpp's own `dflash` arch was found — so no *draft-model* speculator is wired. It does run `spec-type = ngram-mod`, which needs no draft model at all, making that the only speculator this model can currently use.
- Qwen-AgentWorld-35B-A3B (Qwen, Apache-2.0, language world model): 35B-A3B MoE (arch `qwen35moe`, base Qwen3.5-35B-A3B, 40 layers, 256 experts + 8 routed/1 shared active) that simulates 7 agent environments (MCP/Search/Terminal/SWE/Android/Web/OS) via long CoT. Not a coding assistant — it predicts the *next environment state* given an action, so it is the counterpart to an agent, not one. Hybrid attention (10×(3× Gated-DeltaNet → MoE → 1× Gated-Attention → MoE)) means only ~10 of 40 layers carry a growing KV-cache, so long contexts are cheap on VRAM. `UD-IQ4_XS` (16961 MiB) is the smallest 4-bit build that keeps quality; the next steps up (`UD-Q4_K_S` 20.9 GB, `UD-Q4_K_M` 22.1 GB) would not leave room for a 262k KV cache. **Measured on hermine 2026-08-05** with four real loads at KV `q8_0`/`q8_0`, `fit = off`: the full native `ctx-size = 262144` holds at **21082–21104 MiB of 23028** (~1930 MiB free) with **6773–6944 t/s prompt** and **139.3–142.8 t/s generation**, no CPU offload. Unlike Ornith-1.0-35B there is **no cliff**: 196608 (2815 free) and 131072 (3683 free) measure 137.1 and 139.6 t/s, i.e. flat, so nothing is gained by shrinking. `fit = on` was tried too and behaves identically here (139.3 t/s, no offload), so `fit = off` is a no-op as measured — it is set anyway so that a future VRAM shortfall fails loudly instead of silently switching to the offloaded mode that cost Ornith-1.0-35B ~30 %. Shrinking ctx would also contradict the card, which advises **at least 128K** because the model uses extended context for multi-turn simulation. `cache-type-v` was `q4_0` and is now matched to K — that was the section's only real defect. Vendor sampling confirmed verbatim from the card: `temp 0.6`, `top-p 0.95`, `top-k 20`; thinking mode is on by default (`<think>…</think>`) → `jinja = true` + `reasoning-format = auto`, and the card's reasoning parser is `qwen3`. It also recommends ~32768 output tokens per query and ships domain-specific system prompts in the GitHub repo's `prompts/` — both are client-side, not preset keys.
- Gemma 4 31B (Google/unsloth, Apache-2.0, dense QAT VLM): dense 31B (arch `gemma4`, 60 layers, native 256K ctx). Hybrid attention interleaves local sliding-window (1024) layers with periodic global layers (unified K/V + Proportional RoPE), so the growing KV-cache lives only in the few global layers → long contexts are cheap on VRAM. Quantization-aware-trained `UD-Q4_K_XL` (17.3 GB) keeps near-bf16 quality and fits a 24 GB card alongside the ~280 MB MTP drafter `mtp-gemma-4-31B-it.gguf`. MTP speculative decoding is wired via `spec-type = draft-mtp` + `model-draft = …/mtp-gemma-4-31B-it.gguf` (lossless — the target verifies every drafted token). Presets differ per machine: **lieselotte** (7900 XTX, Vulkan) `device = Vulkan0`, `ctx-size = 163840`, KV `q8_0`/`q4_0`, `spec-draft-n-max = 4`; **hermine** (RTX 4090, 23028 MiB, CUDA) `ctx-size = 65536`, KV `q8_0`/`q8_0`, `spec-draft-n-max = 3` — measured 2026-08-04 at 22467 MiB peak (~560 MiB free), 2391 t/s prompt / 86 t/s generation. Native max is 262144, but on hermine `llama-fit-params` caps a V-`q8_0` run at 58624 with its default 1 GiB margin; the KV cache is preallocated in full at load, so 65536 is a deliberate low-margin choice that assumes nothing else claims VRAM. Text+Image via optional `mmproj = …/mmproj-F16.gguf`. Vendor sampling: `temp 1.0`, `top-p 0.95`, `top-k 64`; thinking is opt-in via a `<|think|>` token → `jinja = true`. Two follow-ups from the 26B-A4B work were settled here by measurement on 2026-08-05, five real loads at ctx 65536: **`cache-type-k/v-draft` saves no VRAM at all** — 22265–22269 MiB at load and 22277–22281 MiB after generation, identical to within 4 MiB whether the draft cache is f16 or `q4_0`. The assumption behind adding it (a separate drafter cache sitting next to the target's) is wrong for MTP: the drafter shares the target's KV cache, which is what the 26B's card states outright. It is set to `q4_0` anyway because it costs nothing and generation was faster in all three `q4_0` runs (80.45 / 80.00 / 72.41 t/s) than in both f16 runs (66.90 / 69.20), tracking draft acceptance (0.849 / 0.849 / 0.727 against 0.656 / 0.684). **Do not quote a percentage**: an initial pair of bit-identical `q4_0` runs suggested a clean +16 %, and the third run landed between the two clusters, so n is too small and the spread too wide for anything beyond "not slower, probably a little faster". All five prompts were the same short one, so the prompt-throughput figures (133–169 t/s) are not comparable to the 2391 t/s above, which used a large prompt. `cache-reuse = 256` is **inert** here too — `cache_reuse is not supported by this context, it will be disabled` in 5 of 5 logs — which later turned out to be true of every model on this build, see the dedicated bullet below. `ctx-size` stays 65536, and a second attempt to improve it on 2026-08-05 also came back negative: with `fit = off` the section runs 77.3 t/s at 577 MiB free, 78.2 at 1540 (ctx 49152) and 78.1 at 2508 (ctx 32768) — **flat**, no CPU offload anywhere. Giving up context buys nothing here, so the thin margin is a robustness question only, not a speed one. This is also the measurement that killed the free-VRAM explanation for the Ornith-1.0-35B cliff.
- Gemma 4 26B A4B (Google/unsloth, Apache-2.0, MoE QAT VLM): 25.2B total / 3.8B active MoE (arch `gemma4`, 30 layers per the card but `n_layer 31` in the GGUF, 8 of 128 experts active + 1 shared, sliding window 1024, native 256K ctx). Same hybrid attention as the 31B, and the load log shows the consequence directly — `llama_kv_cache: layer 3: sharing with layer 29` — which is why the full native context is affordable. `UD-Q4_K_XL` (13.3 GB) plus the 252 MB drafter `mtp-gemma-4-26B-A4B-it.gguf`. **The drafter was the find**: the card documents `--spec-type draft-mtp --spec-draft-n-max 4`, but only the main GGUF had been downloaded and `models.list` carried a single line for this repo, so `recommend.sh` correctly reported "no MTP head found" and the section ran `ngram-mod` alone. Both are fixed. Measured on hermine 2026-08-05 by actually loading the server at `ctx-size = 262144`, KV `q8_0`/`q8_0`, draft KV `q4_0`/`q4_0`, `spec-draft-n-max = 4`: **21193 MiB peak of 23028** (~1835 MiB free), **8109 t/s prompt** (7828-token prompt), **234.6 t/s generation** (400 tokens), and MTP **draft acceptance 0.751** with mean draft length 3.99 — the budget of 4 is fully used, so it is not set too high. After a long prompt acceptance drops to 0.547 / mean 3.13 and generation to 158.6 t/s. Roughly 3× the 31B's prompt and 2–3× its generation throughput at 3.8B active parameters, and unlike the 31B it holds the full 262144 rather than 65536. Two caveats on those numbers: the runs where generation was exercised omitted `cache-reuse` and read 20766–20778 MiB, ~430 MiB below the load that included it — **unexplained**, and the higher figure is quoted because it matches the written section. And `llama-fit-params` alone had reported 18031 + 240 MiB, a lower bound, because it cannot size a separate drafter's context; only the real load settles it. Sampling is one single published set for all use cases — `temp 1.0`, `top-p 0.95`, `top-k 64` — already in the section. **Thinking must not be forced on here.** `recommend.sh` flagged that the template consumes `reasoning_content` from history and suggested `reasoning = on`, as with Laguna; the card says the opposite in so many words ("Thoughts from previous model turns must *not be added* before the next user turn begins") and makes thinking opt-in per request via a `<|think|>` token in the system prompt. So this is not the Laguna case, `reasoning = on` stays out, and `jinja = true` + `reasoning-format = auto` are enough. `cache-reuse = 256` is kept but is **inert**: the server logs `cache_reuse is not supported by this context, it will be disabled`. It is left in so it starts working if a future build supports it — the limitation turned out not to be architectural at all, see the dedicated bullet below. Text+Image per the card (~550M vision encoder), but no `mmproj-*.gguf` is present, so the preset is text-only.
- Ternary Bonsai 27B (Prism ML, Apache-2.0, ternary quant of Qwen3.6-27B): dense 27B, arch `qwen35`, 65 layers, native 262144. End-to-end **ternary** weights ({-1,0,+1}) at a true 1.71 bpw, 7233 MiB on disk — a third of what the other 27B-class models here cost, and the card claims 94.6 % of FP16 quality (80.49 vs 85.07 across 15 thinking-mode benchmarks) against 85.5 % for a conventional IQ2_XXS build. **Pick the right pack**: the repo ships three 2-bit files and only one works on stock llama.cpp. `Ternary-Bonsai-27B-Q2_0.gguf` is g128 and needs Prism's own fork (the card's quickstart clones it); `Ternary-Bonsai-27B-Q2_g64.gguf` is the group-64 pack the card describes as "matching the 64-value-group Q2_0 packing in llama.cpp", and build 10243 loads it in 3.3 s with no errors — verified 2026-08-05. `PQ2_0` is a third variant the card never explains and was not tried. **Measured on hermine 2026-08-05** at `ctx-size = 262144`, `fit = off`: KV `q8_0`/`q8_0` gives **18663 MiB of 23028** (4365 MiB free), KV `q4_0`/`q4_0` gives 14590 MiB (8438 free) — and both run **70.0-70.3 t/s generation** / ~3120 t/s prompt, i.e. the KV type is free here. The presets therefore use `q8_0` on hermine and keep the Vulkan `q4_0` floor in the example, even though the card standardizes on a 4-bit KV cache: that recommendation exists to fit laptop memory budgets, and on a 23 GB card the better cache costs nothing measurable. **DSpark is out, and the reason is a repo policy rather than a dead end.** The card ships a DSpark drafter and advertises a lossless 1.34x CUDA decode speedup at draft depth 4; `llama-server --spec-type` on build 10243 even lists `draft-dspark`. It cannot work here, and the investigation on 2026-08-05 went three layers deep before that was certain. First the drafter failed to load with `gguf_init_from_reader: tensor 'dspark.fc.weight' has offset 337718592, expected 357584192` — 19865600 bytes, exactly 1 byte per 64-element block. The file was **not** at fault: byte-complete at 1946393568 bytes, header declaring `general.architecture = dspark` and `dspark.block_count = 6`. The cause is that the two shipped files pack the *same* ggml type 42 differently, and each toolchain says so itself — the Prism fork documents `Q2_0 : 2.125 bpw` (17 bytes/block), upstream documents `Q2_0 : 2.25 bpw (group 64)` (18 bytes/block). Hence the mirrored errors: upstream rejects the drafter, and the fork rejects the `Q2_g64` main model with `'output_norm.weight' has offset 357580800, expected 337715200`. Repacking the drafter (recomputing offsets and substituting the target's correctly packed `token_embd.weight`, which the card says the drafter shares anyway) fixed the container — every offset then matched — and revealed the real wall: `unknown model architecture: 'dspark'`. **Upstream does not implement the dspark architecture at all**; the flag exists in the argument parser only. Release b10297 (2026-08-06) mentions it zero times. The only working combination is the Prism fork plus the g128 `Ternary-Bonsai-27B-Q2_0.gguf`, and the fork does publish prebuilt Windows CUDA-12.4 binaries, so no compiler would be needed — but **this repo uses official llama.cpp builds only** (see Conventions), so that path is closed by choice, not by capability. The section therefore runs `ngram-mod`. Do not re-litigate this without first checking whether an official release has added the `dspark` arch; the drafter GGUF and the `models.list` line for it have been removed. Vendor sampling: `temp 0.7`, `top-p 0.95`, `top-k 20` (the settings behind all reported thinking-mode numbers). Vision is available via an optional mmproj (629 MB `Q8_0`), not downloaded, so the preset is text-only. llama.cpp reports `chat template supports preserving reasoning` here as it does for Ornith; the card documents neither preserved thinking nor a system prompt beyond "You are a helpful assistant", so no `reasoning = on`.
- Muse-Glimmer-30B (Meta, Apache-2.0, agentic multimodal): dense 29.6B, arch `muse-glimmer`, 52 layers, native 131,072 context. Hybrid attention `[Local, Local, Local, Global]` repeating with sliding window 2048, so only 13 of 52 layers carry a growing KV-cache (2 KV heads, head dim 128, GQA 16:1). `kquant-17gb` is 15980 MiB on disk, the vendor's own 24 GB target (a `kquant-dynamic` at 19.7 GB targets 32 GB and is not downloaded). Card puts the quantization cost at 1.0 % averaged over 15 benchmarks against full precision.
  **The arch landed between two builds on the same day, so read version numbers here carefully.** b10344 (newest release for most of 2026-08-10) rejects `unknown model architecture: 'muse-glimmer'`: PR #26841 merged as commit `62bf73d` at 11:07 UTC, while b10344 targets `7a20b417f` from 08:25 UTC. Upstream says the same in issues #26858 and #26865, and the "the ROCm build works" claim in #26865 was retracted by its own reporter — the arch check is backend-independent frontend code, so no b10344 asset can load it. **b10353 (`f8def7fe1`) does contain it**, and `server.sh`'s auto-update pulled that build the same evening, which is when everything below became measurable. If a preset here ever fails with the arch error again, check `llama-server --version` before anything else.
  **Measured on hermine 2026-08-10, build b10353, `ctx-size = 131072` with the mmproj resident on the GPU:** **21717–21726 MiB of 23028**, i.e. **882–892 MiB free** across three stable samples. The previous configuration (`ctx-size = 65536`, mmproj offloaded to host RAM) measured 19575–19619 MiB with 2990–3453 MiB free. Both were real loads with all three stages up — the router log shows `text_model`, `spec_model` and `mmproj_model` each reaching 1.0, and `/props` on the child reports `n_ctx = 65536`/`131072` and `"vision": true`. Note that **router mode suppresses llama.cpp's own `llama_kv_cache` and buffer-size lines**, forwarding only load-progress state, so the per-buffer breakdown is *not* available this way; the VRAM total is the authoritative figure and a single-model run would be needed to itemise it.
  **The 892 MiB margin is a deliberate, informed choice and thinner than this repo's own guidance.** The working target elsewhere in this file is ~1.5 GiB, and 131072 was picked over 98304 (~1.1 GiB free, projected) and over moving the mmproj back to host RAM (~2.2 GiB free, projected) with the risk accepted explicitly: a single image can carry up to 4096 visual tokens and allocates vision compute buffers at request time, which is exactly what this margin has to absorb. If image-heavy use ever OOMs, set `mmproj-offload = false` first — that is the cheapest lever and it keeps vision working.
  **A KV-per-token estimate got this wrong by ~700 MiB, which is why the number above is a load and not a calculation.** The estimate reasoned that only 13 of 52 layers are global and therefore grow with context (2 KV heads × 128 head dim at `q8_0` ⇒ ~6.9 KiB/token, 926 MiB at 131072) and projected ~1600 MiB free with the mmproj resident. Actual: 892. The 13-global-layer model itself is sound and was confirmed the other way round — at 65536 the whole KV+compute bucket measured ~1030 MiB, whereas allocating full context on all 52 layers would have needed 1.85 GiB of KV alone, so the sliding-window optimisation is provably active. What the estimate missed is that a resident mmproj costs materially more than its 1335 MiB file and that compute buffers grow with context.
  **What was verified before the arch existed**, and is worth keeping because it isolates key-name validity from arch support: on b10344 the router loaded all 10 custom presets, `/v1/models` listed `Muse-Glimmer-30B`, and a chat request failed with *only* `unknown model architecture` — no argument or flag error anywhere. So every key name in the section is accepted, including `mmproj-offload`, `cache-type-k/v-draft`, `cache-ram` and `spec-type = draft-dflash,ngram-mod`.
  **`spec-draft-n-max = 16`, not the 4 this repo uses everywhere else.** That 4 was inherited from the MTP sections and is wrong here on the vendor's own numbers: DFlash is a block-diffusion drafter that "predicts entire blocks of 16 tokens in a single forward pass", so a budget of 4 discards 12 of every 16 drafted tokens and caps the advertised 3.1× speedup (RTX 5090, 74.9 → 233.4 t/s). `--help` reports the flag's default as 3, which is tuned for one-token-per-head MTP drafters and is equally wrong for a block drafter. 16 is mechanism-derived, and the **measurement on 2026-08-10 vindicates the direction without reaching the value**: mean accepted draft length is **6.03–6.17** at short context (acceptance 0.337–0.344), so the inherited 4 would provably have clipped it, while 16 is never approached — the binding constraint is acceptance, not the budget. **Acceptance collapses with context length**: 0.34 / mean 6.17 at ≤2.2k tokens, 0.129 / mean 2.93 at a 31.6k prompt, 0.098 / mean 2.47 on a long mixed task. Generation tracks it — **128.2 t/s** at ~70 tokens of context, 117.0 at 2.2k, 55.5 at 31k, 46.5 on the long task — against a prompt throughput of 2400–2550 t/s throughout. So the card's 3.1× headline (RTX 5090, 74.9 → 233.4 t/s) describes short-context behaviour; do not expect it to survive a full agent context on this box.
  **`cache-type-k/v-draft = q4_0` matters here, unlike the MTP sections.** The gemma-4-31B measurement below found the draft cache types save no VRAM at all, because an MTP drafter shares the target's KV cache. DFlash does not — it is a separate model behind `model-draft` with its own attention (sliding window 2048 over all layers, 32 query / 8 KV heads, sequence length 131072), and `--help` puts the draft cache default at **f16**. Quantizing it should therefore save real VRAM rather than nothing. Both are set to the same type because the CUDA matched-pair rule presumably applies to the drafter's fused kernel too (untested), and `q4_0` rather than `q8_0` because speculative decoding is lossless: a worse draft cache can only lower the acceptance rate, never corrupt output, which is the right way to spend a tight budget.
  **The mmproj stays resident on the GPU, and the reasoning that first said otherwise was wrong.** Vision needs `mmproj-kquant.gguf` (1335 MiB, ViT-G/14 perception encoder) and `--mmproj-offload` defaults to *enabled*, i.e. in VRAM, so no key is set. The section originally carried `mmproj-offload = false`, argued from a loose anchor: file sizes sum to 15980 + 1555 + 1335 = 18871 MiB of 23028, and the only measured comparable was gemma-4-31B, which peaks at 22467 MiB with 560 MiB free at ctx 65536 while carrying ~1.3 GB *fewer* weights. That anchor pointed the right way about *cost* but the wrong way about *feasibility*: Muse-Glimmer's KV is far cheaper per token, so the real load at 131072 with the projector resident fits at 892 MiB free. Dropping `mmproj` entirely, as the other VLM sections here do, would free the same VRAM and lose vision outright; that was considered and rejected, and unlike those sections the file is actually on disk here.
  `cache-ram = 16384` is set, matching hermine's other agentic-coding sections — the card's intended use is explicitly coding agents and long-horizon tool use. Embedded template consumes `reasoning_content` from history (preserved thinking), so `reasoning-format = auto` + `jinja = true`; **no `reasoning = on`**, because unlike Laguna the template was not found to default thinking off, and the card controls effort through the *system prompt* instead (`Reasoning strength: low|medium|high|xhigh`, "use high or xhigh for complex problem solving, coding, and agentic tasks"). That is a client-side string, not a preset key — OpenCode has to send it, and nothing in this repo does yet. Vendor sampling is one single published set, `temp 1.0`, `top-p 0.95`, `top-k 64`, adopted whole. `ngram-mod` is chained after `draft-dflash` for consistency with every other section, but note the interaction is **unexamined**: llama.cpp hardcodes all `ngram-*` ahead of all `draft-*`, so ngram-mod preempts the DFlash block draft whenever it finds a ≥48-token verbatim chain, and it also raises the global draft budget to 64. On a model whose headline feature is the DFlash speedup that could help or hurt; nobody has looked.
- Nemotron-3.5-Lightning-30B-A3B (NVIDIA, OpenMDW-1.1, agentic reasoning/chat): 30B-A3B MoE, arch `nemotron_h_moe`, **53 layers of which only 7 carry attention** (`attention.head_count_kv` is a per-layer list reading 2 for seven layers and 0 for the other 46), the rest being Mamba-2 SSM layers with constant-size recurrent state. 128 experts, 6 routed + 1 shared active, native `context_length = 1048576`. That layout is the whole story of this section: `llama-fit-params` puts the KV cache for the **full 1M context** at 3311 MiB, so context is nearly free and VRAM is spent on weights instead.
  **The URL this work started from was the wrong checkpoint, and the difference is not cosmetic.** `nvidia/NVIDIA-Nemotron-3.5-Lightning-30B-A3B-Base-BF16` is the *base* checkpoint — the card says "no supervised fine-tuning, reinforcement learning, or distillation has been applied". No chat template, no tool calling, and no MTP drafter GGUF exists for it (only `mradermacher` quantizes it at all). The post-trained `…-30B-A3B-BF16` is what this section serves. If a future reader follows the Base link, that is why it was not used.
  **Quant choice is forced by a tensor shape, not by taste.** `expert_feed_forward_length = 1856`, which is **not a multiple of 256**, so every K- and IQ-superblock type (256-element blocks) is unrepresentable on the expert tensors and llama.cpp falls back to a 32-element-block type. The experts are 15758 MiB of the 18046 MiB file, which is why the whole quant ladder collapses onto one size: bartowski's IQ2_XXS is 17965 MiB and IQ4_NL is 18046 MiB — **81 MiB apart**, while Q4_1 jumps to 19905 MiB and Q4_K_S to 23.20 GB. Everything below IQ4_NL is therefore *strictly dominated*: same size, worse weights. Verified by parsing the GGUF tensor table — in the IQ4_NL file the experts land on `IQ4_NL` (46 tensors) with 2 on `Q4_0`, i.e. the best 4-bit 32-block type available. `bartowski` over `unsloth` because unsloth ships no MTP sidecar at all; that turned out not to matter (see below) but was not knowable in advance.
  **The MTP drafter is a large net loss and is deliberately not wired.** This is the opposite of every other MTP section in this file. Measured 2026-08-15 on hermine, build 10424, ctx 131072, KV `q8_0`/`q8_0`, `fit = off`: with `model-draft = mtp-…-Q8_0.gguf` + `spec-type = draft-mtp,ngram-mod` the load sits at **22303 MiB and generates 86.7 t/s**; dropping the drafter gives **19054 MiB and 195.1 t/s**. That is **3249 MiB** of VRAM — far more than the drafter's 2075 MiB file, because it allocates its own context — bought at **2.25× slower generation**. Draft acceptance was not the problem (0.723 at ctx 262144, mean length 3.87 against a budget of 4, i.e. the budget was nearly saturated). The mechanism is that the target is an A3B MoE with only ~3B active parameters while the MTP block is a ~2 GB dense model, so a drafter forward pass costs roughly what a target forward pass costs and speculation cannot win. Confirmed at three context points with the drafter (86.7 / 105.4 / 95.7 t/s at 131072 / 196608 / 262144) against three without (195.1 / — / 175.1). **Do not re-add `draft-mtp` here without re-measuring**; this paragraph claimed a `# NOTE` in both preset files points here, and the note's absence is now explained: the user deletes comments from `models.ini` deliberately (see the comment-policy bullet under llama.cpp config notes), so warnings must live here and in `models.example.ini`, never in `models.ini`. Also recorded so nobody repeats it: bartowski's `mtp-…-Q4_0.gguf` **fails to load outright** (`failed to load draft model`) while the Q8_0 one loads, cause not investigated because the feature was being dropped anyway.
  **`ctx-size = 262144`, chosen against a measured 524288.** Real loads without the drafter: 131072 → 19054 MiB (3974 free) / 195.1 t/s, **262144 → 19985–20476 MiB (3043 free) / 175.1 t/s**, 524288 → 21518 MiB (1510 free) / 163.6 t/s. The full native 1048576 does not fit (`llama-fit-params` 22530 MiB plus a ~1172 MiB desktop baseline exceeds 23028). 524288 would have hit this repo's stated ~1.5 GiB headroom target exactly and was rejected on the repo's other rule, "throughput beats context": it taxes every generated token by 6.6 % to buy a window that also sits outside NVIDIA's own validated envelope (the card validates 256K on a single H100 and reports RULER 256K 76.88 against RULER 1M 69.62). There is **no throughput cliff** in this range, unlike Ornith-1.0-35B — the degradation is smooth. `fit = off` is a no-op as measured and is set only so a future shortfall fails loudly.
  **Compute buffers do not grow with prompt length here, which is worth knowing because the Muse-Glimmer section was burned by assuming they might.** A 141389-token prompt at ctx 524288 measured 21471 MiB peak against 21522 MiB idle — i.e. *nothing*; at 262144 it was 20476 against 20269, +207 MiB. Prompt throughput on that real prompt is 6567–7004 t/s and generation at 141k of context still 102.8–111.1 t/s. The 30-token probes used elsewhere in this file report prompt throughput as 80–225 t/s and are pure noise; ignore them.
  Sampling is one published set and the GGUF agrees with the card: `general.sampling.temp = 1.0` and `general.sampling.top_p = 0.95` are baked into the file's metadata, and **no `top_k` is published**, so none is set — do not import the `top-k 20` from the neighbouring Qwen sections. Thinking is **on by default and verified rather than assumed**: the embedded template reads `enable_thinking = enable_thinking if enable_thinking is defined else True`, so unlike Laguna no explicit `reasoning = on` is needed, and `jinja = true` + `reasoning-format = auto` suffice. The template also consumes `reasoning_content` from history and llama.cpp suggests `--reasoning-preserve`; not set, following the Ornith precedent that the card must confirm it. **The card's coding-agent advice does not apply to this GGUF**: it tells agents to send `chat_template_kwargs: {"force_nonempty_content": true}`, but that key does **not exist** in bartowski's embedded template (checked), so setting it would be a silent no-op. `cache-ram = 16384` matches hermine's other agentic sections. `spec-type = ngram-mod` is kept — no draft model, no VRAM, lossless — but its value here is **unmeasured**: the A/B run was started and aborted, so this is the same mechanism-only assumption as every other section, not a measurement.
- Enabled providers: `lieselotte`, `hermine`, `anthropic`, `openrouter` (a fifth, `elektronengehirn`, appeared in `opencode.jsonc` from outside this session and is undocumented here)
- `openrouter` is the built-in OpenRouter provider (models preloaded from Models.dev); its API key is read from the `OPENROUTER_API_KEY` env var via `"apiKey": "{env:OPENROUTER_API_KEY}"` instead of `/connect`
- Plugin (npm): `opencode-claude-auth@latest`; `share: disabled`
- Bash guard: **no longer shipped from this repo.** The merged `command-guard.js` lives in `stadtwerk_ai_config/.opencode/plugin/`; that repo's `install-opencode.sh` drops it into the same `~/.config/opencode/plugin/`, so it applies in every project. It hooks `tool.execute.before`, **normalises** each command (strips `env`/`sudo`/`nice`/`timeout` wrappers, resolves absolute paths & `$(which x)` to bare verbs, strips `.exe`, decodes base64 as UTF-8 *and* UTF-16LE for PowerShell's `-EncodedCommand`, reads here-docs, re-scans `-c`/`-Command` payloads of sh/python/node/powershell/cmd) and **recursively splits** it across pipes/`&&`/`;`/subshells before matching — which is what closes the string-glob bypasses the wren.wtf "Stop Using OpenCode" post calls out. Rules are **embedded in the `.js`** (an external rule file would not survive: the team installer copies `plugin/*.js` only, and a missing rule file used to disable guarding silently) and are **deny-only**. The ask tier belongs to the team config, because a plugin cannot prompt: the `permission.ask` hook is declared in `@opencode-ai/plugin` 1.14.20's types but has **zero dispatch sites** in the opencode 1.18.12 binary (verified by enumerating every `trigger("…")` literal). Blocked: recursive delete of `/`~/`$HOME`/system paths, filesystem/partitioning tools, raw device writes, download-piped-to-shell, private-key reads, fork-bombs, shell redirection into CI workflows / `clusters/**` / Terraform, and destructive kubectl/helm **against prod** — the context comes from an explicit `--context` flag if present (otherwise `kubectl --context prod delete` would bypass it while test is active), else from `kubectl config current-context`, resolved lazily so reads stay fast. Fail-closed twice: destructive + undeterminable context, and base64-for-a-shell that will not decode. Health signal: the toast **"Löschschutz aktiv"** at startup plus `echo guard-selftest`, which must be blocked — a plugin that fails to load cannot warn about itself, so only a positive signal makes its absence detectable. Verified 2026-08-04: 33/33 unit cases, and 0 false positives across 2253 distinct real commands from this machine's log — **but the zero no longer holds**: on 2026-08-10 `rm -f /tmp/opencode/a.json /tmp/opencode/b.json /tmp/opencode/c.json` tripped `rm-rf-dangerous-target`, with no `-r`, no glob and three explicit paths under `/tmp`. Splitting it into three single-target `rm -f` calls passed. So the rule appears to key on argument count or the plain `rm -f` verb rather than on a dangerous target, which is a false positive in the literal sense the rule name claims to exclude. Not a sandbox — it cannot see inside a shell script it invokes, and writing *about* its rules in a here-doc can trip them.
- Permissions come from the **team** config `~/.config/opencode/opencode.json` (installed by `stadtwerk_ai_config`), *not* from the personal `opencode.jsonc` — a `permission` block there would override the team baseline, which that installer explicitly forbids. The baseline is default-**ask** (`bash: {"*": "ask"}`) with an allow-list of ~116 read-only patterns, `edit: allow` except CI workflows / `clusters/**` / Terraform, and a `read` deny-list covering `.env*`, `.ssh/**`, `*.pem`, `.aws/credentials` and kubeconfigs. Measured 2026-08-04 against 6758 logged bash calls from this machine: 34.6 % would prompt (62.6 % before the allow-list was widened by 14 patterns, `echo *` alone accounting for 1547 calls).
- MCP servers: `atlassian` (remote OAuth at `https://mcp.atlassian.com/v1/mcp`) and `playwright` (local via `npx @playwright/mcp@latest`)

## Claude Code config notes

- MCP server: `chrome-devtools` (local via `npx chrome-devtools-mcp@latest`)
- Custom commands: `claude-backup/.claude/commands/commit-push.md`
- Skills: `c4-devops-ticket` (Jira ticket creation for DO project)

## llama.cpp config notes

- Linux/Pop!_OS adaptation of `countzero/windows_llama.cpp`; **Vulkan** backend, **prebuilt** binaries (no compiler/conda).
- Scope: run already-built GGUF models only (no quantization/conversion).
- `bootstrap.sh` downloads `llama-<tag>-bin-ubuntu-vulkan-<arch>.tar.gz` into `vendor/` (idempotent, `--force` to reinstall).
- `download-model.sh` pulls GGUFs to `$LLAMA_MODELS_DIR` (default `~/.local/share/llama.cpp/models`, outside the repo); prefers `hf`/`huggingface-cli`, falls back to `curl`; manifest `models.list`.
- `server.sh`: router mode (default, `presets/models.ini`) or single model; serves OpenAI-compatible API at `LLAMA_HOST:LLAMA_PORT` (default `127.0.0.1:8081`).
- `LLAMA_VERSION` defaults to `latest`. `server.sh`/`server.ps1` auto-run `bootstrap.*` before serving to pick up new releases, throttled by a `vendor/.last-auto-check` marker (`LLAMA_UPDATE_CHECK_INTERVAL`, default 3600s) so repeated restarts don't hit GitHub's API. `LLAMA_AUTO_UPDATE=0` disables it (offline use); a failed check falls back to the already-installed build instead of hard-failing.
- Config in `config.env` (gitignored; copy from `config.env.example`). On the Windows host there is no `config.env` — it configures itself in `config.ps1` (also gitignored, and it holds a plaintext `HF_TOKEN`). All four bash entry points therefore load their settings through `config-load.sh`, which resolves env var > `config.env` > `config.ps1` > `config.env.example`. Before that existed, `server.sh`, `download-model.sh` and `bootstrap.sh` were **completely broken on hermine** (verified 2026-08-05): they fell through to `config.env.example`, which carries CRLF, and `source` then fails on every line with `$'\r': command not found`. Falling through was wrong twice over — that file also assigns a Linux models path and `LLAMA_BACKEND=vulkan`, so a working `source` would have pointed at a nonexistent directory and fetched a Vulkan build for a CUDA box. `recommend.sh` keeps its own copy of this logic on purpose: it must run standalone from the skill directory.
- `server.sh` could not actually **serve** on hermine until 2026-08-05, only `--list`. Two separate defects, both found by running it: `resolve_bin` searched for `llama-server` and not `llama-server.exe`, and once that was fixed the Windows binary got the POSIX `/mnt/c/...` models dir and died with `does not exist or is not a directory`. Paths handed to an `.exe` now go through `host_path()` (`wslpath -m`) while bash keeps the POSIX form for its own `find`/`-f` tests. Verified end-to-end: the router logs `Loaded 8 custom model presets` and `/v1/models` lists all eight (plus one entry from llama.cpp's own HF cache, which is not ours).
- `fit = off` is set in **all twelve** sections of both preset files, but it is only *load-bearing* in `Ornith-1.0-35B`, where `fit = on` silently moves MoE experts to CPU and costs ~30 %. Measured elsewhere it is a no-op: gemma-4-31B and Qwen-AgentWorld show no `tensor overrides to CPU` either way. It is set uniformly so a VRAM shortfall fails loudly instead of silently switching to a slower configuration — before 2026-08-05 it was present in 4 of 8 sections purely by accident of which ones had been touched.
- Two WSL-on-Windows traps when testing a server by hand on hermine, both cost a wasted run: `--host 127.0.0.1` binds the *Windows* loopback and is unreachable from WSL (use `0.0.0.0` plus the gateway IP from `ip route`; the name `hermine` does **not** resolve from inside WSL on hermine itself, only from the LAN). And `nohup ./llama-server.exe &` returns the *bash* PID — `taskkill /PID` on it leaves the Windows process alive holding VRAM, which then makes the next start fail with `failed to load draft model`. Kill by image name (`taskkill /F /IM llama-server.exe`) and wait for `nvidia-smi` to drop — but **only when no other llama-server is meant to stay alive**: killing by image name also takes down the user's router. When one is running, get the PIDs from `tasklist.exe` before starting a test instance and kill only your own.
- **hermine drives its display from a GT 610 since 2026-08-15; the 4090 idles at ~22–44 MiB.** Every VRAM figure in this file measured before that date sat on a 394–1172 MiB desktop baseline, so old "free VRAM" numbers understate what fits today — re-measure before trusting them at the margin. The install itself broke the machine first, and the fix is worth recording because Windows Update *will* try to reproduce the breakage: the GT 610 is Fermi, its last driver line is 388/391, and WU auto-installed `nv_ref_pubwu.inf` (388.13) on detection. Windows cannot load two NVIDIA KMDs, so the 4090 went to Code 31 (later 39) and `llama-server --list-devices` reported *no devices at all* — while `device = CUDA0` in the presets was never the problem and still names the 4090 (the GT 610 has no CUDA support and is invisible to llama.cpp; there is nothing to pin). Fix, verified: `pnputil /delete-driver oemXX.inf /uninstall` on the legacy package (it was `oem118.inf`; re-enumerate with `pnputil /enum-drivers`, the giveaway is `nv_ref_pubwu.inf` + version 23.21.13.8813), reboot; if the 4090 then shows Code 39, `pnputil /remove-device "<instance-id>"` + `pnputil /scan-devices` rebinds it to the store's modern driver (`nv_dispi.inf`) without any download. Target state in Device Manager: 4090 = NVIDIA driver OK, GT 610 = **Microsoft Basic Display Adapter** — never install an NVIDIA driver for the GT 610. If the 4090 dies again after a WU cycle, it is this, same fix.
- On CUDA (hermine), `cache-type-v` MUST equal `cache-type-k`. Measured 2026-08-04 with gemma-4-31B @ 65536 + MTP: matched `q8_0`/`q8_0` yields 2391 t/s prompt / 86 t/s generation, the mixed `q8_0`/`q4_0` pair collapses to 32 t/s / 9 t/s because it falls off the fused FlashAttention kernel. Untested on Vulkan (lieselotte), whose presets still use the skill's `q8_0`/`q4_0` floor — so the llama-preset skill's V-floor is not safe to apply blindly on the CUDA box.
- `spec-type` carries `ngram-mod` in **every** section of both `presets/models.ini` and `presets/models.example.ini` (12 each) — as `draft-mtp,ngram-mod` where an MTP drafter exists, as `draft-dflash,ngram-mod` for Muse-Glimmer, as bare `ngram-mod` where none does (both Ornith, Qwen-AgentWorld, Laguna) or where one exists but measured slower (Nemotron-3.5-Lightning). It needs no draft model and no VRAM, and speculative decoding is lossless, so a bad fit costs speed and never output quality. The gain is **unmeasured on both machines** — adopted from countzero's 24 GB preset on mechanism. Expect it to pay off on file-rewrite-heavy agent turns and to do nothing on novel prose, where the 48-token minimum chain length suppresses the draft entirely. `cache-ram = 16384` is set on hermine's four agentic-coding sections (both Qwen3.6, Laguna, Muse-Glimmer) and nowhere else: the default is 8192, the box has 39 GB, and the value applies **per loaded model**, so raising `models-max` above 1 multiplies it.
- Models are **never** committed: `.gitignore` excludes `llama.cpp/{vendor,cache}/`, `config.env`, `presets/models.ini`, and `**/*.gguf`.
- **`presets/models.ini` is kept comment-free — the user deletes `#` comments from it on sight** (global AGENTS.md register rule: a comment must say something the keys cannot; a machine-local working file is not documentation). Any rationale a section needs goes into `models.example.ini` (tracked, comment-friendly) and the model's bullet in this file. Discovered 2026-08-15 when freshly written NOTEs vanished from `models.ini` and a server-rewrite hypothesis was disproven by the user simply saying they had deleted them; the same mechanism explains the Nemotron NOTEs that this file once claimed existed.
- OpenCode: the local `lieselotte` provider `baseURL` is `http://127.0.0.1:8081/v1`; the remote `hermine` provider points at `http://hermine:8081/v1`.
- Skill `.opencode/skills/llama-preset/` (`scripts/recommend.sh`) — **moved out of `.claude/skills/` on 2026-08-04 and substantially rewritten.** It generates/updates a `presets/models.ini` `[section]` from measured hardware (`llama-server --list-devices`, `llama-fit-params`, iGPU deprioritized) plus agentic defaults. What changed and why, because each item was a real defect found by testing it against this machine rather than reading it:
  - It was **invisible to OpenCode**: its `description` was 1371 characters against a documented 1024 limit, so it never appeared in `<available_skills>` while `commit-push` in the same directory did. Now 984. Every session before this one ran without it, which is the likeliest reason the presets had drifted so far.
  - `cache-type-v = q4_0` was a global "floor". On CUDA that is the mixed pair that collapses throughput 9× (see the note above), and the skill would have regenerated it. The V type is now **backend-derived** from the device id: `q4_0` on Vulkan, matched to K everywhere else.
  - `spec-type` knew only `draft-mtp` from a hardcoded list. Both the allowed KV types and the speculator list are now **parsed out of `--help`**, so a newer llama.cpp unlocks better defaults with no edit — the point of keeping this box's server current. `ngram-mod` is chained in whenever the build offers it.
  - Nothing inspected the **embedded chat template**, so Laguna's `enable_thinking | default(false)` was invisible. The script now scans it (200 MiB window — the template sits past the tensor-name table) and reports thinking/preserved-reasoning findings instead of silently accepting the card's claim.
  - It could not run on hermine at all: `resolve_bin` matched only `llama-server`, and sourcing the CRLF `config.env.example` broke bash. Worse, passing a `/mnt/c/...` path to the Windows `.exe` makes `llama-fit-params` print its header and **no data line** — a silent failure the script read as "does not fit" and turned into `ctx-size = 4096` for a model that measurably does ~160k. Paths are now converted via `wslpath -m`, config precedence is env > `config.env` > `config.ps1` > `.example`, and CR is stripped.
  - Section names were derived from the GGUF filename (`[Laguna-XS-2.1-IQ4_XS.gguf]`) rather than the bare alias, so every existing model looked new. Combined with a CRLF-blind `$0==sec` match in the merge, `--write` would have appended a **duplicate** section.
  - `--write` **deleted keys it does not manage.** Sampling, `reasoning` and `cache-ram` showed up in the diff as "no longer set" and would have been dropped. They are now carried over verbatim and reported as `= key (kept)`. The merge also replaces in place instead of moving the section to the end of the file, because section order is maintained by hand.
  - `n-cpu-moe` is now **opt-in** (`--allow-cpu-moe`). It used to buy native context by pushing experts to host RAM, which puts a PCIe round trip in the generation path — the wrong trade for long agent sessions. Default is to shrink `ctx-size` until everything is GPU-resident.
  - Sampling guidance changed from "prefer the lower temperature" to "adopt one published set whole, never mix values across sets", which is the rule that keeps a general-set `presence-penalty 1.5` out of a coding preset.
  - A sibling `mtp-*.gguf` counted as a name match, so the moment a model *had* its drafter on disk it became unreachable by alias: `recommend.sh gemma-4-26B-A4B` died with "Ambiguous model", and `gemma-4-31B` had the same defect all along. Drafters are now excluded from target resolution unless asked for by exact filename — the script looks for them a few steps later anyway.
  - The drafter exclusion above was too narrow: it matched only `mtp-*.gguf`, so `Ternary-Bonsai-27B-dspark-Q4_1.gguf` made `Ternary-Bonsai-27B` ambiguous in exactly the same way. Resolution now skips any sibling whose name contains `mtp`, `dspark`, `dflash`, `eagle` or `mmproj` — the draft-* families this build's `--spec-type` lists, plus vision projectors. Naming is vendor convention rather than a standard, so this is a heuristic, and asking for such a file by its exact name still bypasses the filter.
  - `presets/models.example.ini` contained a **duplicate** `[gemma-4-26B-A4B]`, byte-identical, at two positions. Removed the earlier one so the remaining position matches `models.ini`'s hand-maintained order (both files now list the same sections in the same sequence). This is the failure mode the CRLF-blind section match used to produce, so it is worth re-checking after any `--write`.
  Validation: run against `Laguna-XS-2.1` on hermine it independently produced `ctx-size = 159744` versus the 163840 chosen by hand — one 4096 step apart, from an independent path. Still not automated: reading the model card, weighing the VRAM margin, and deciding on the template finding. `temp = 0` remains refused without `--zero-temp-ok`; OCR models (section name matches "ocr") keep conservative defaults; vendor keys arrive via `--extra KEY=VALUE` and never override a hardware/agentic-owned key.

## Conventions

- This repo is a dotfiles/config backup, not a software project. Do not add build tooling or tests.
- `.gitignore` excludes `.claude/settings.local.json`, `.playwright-mcp`, and the llama.cpp `vendor/`, `cache/`, `config.env`, `presets/models.ini`, and `*.gguf`.
- Fresh machine setup order: SSH → clone repo → `bashrc-backup/install.bash` → Claude Code → `claude-backup/install.sh` → `opencode-backup/install.sh` → authenticate MCPs → (optional) `llama.cpp/bootstrap.sh`.
- Only **official** llama.cpp builds go into `llama.cpp/vendor/` — the releases `bootstrap.sh` fetches from `ggml-org/llama.cpp`. Vendor forks are not used even when they ship prebuilt binaries and even when they are the only way to run a model's advertised feature; the Ternary-Bonsai DSpark drafter is the case that established this. `bootstrap.sh` always installs into the fixed `vendor/llama.cpp` and `rm -rf`s it first, so a second build can never appear there by accident — which is what keeps the plain `find vendor/ -name llama-server*` in `server.sh` and `recommend.sh` unambiguous.
- Any work on `llama.cpp/presets/*.ini` — adding a section, retuning an existing one, or diagnosing a model's context/speed — goes through the `llama-preset` skill (`.opencode/skills/llama-preset/`) **first**. This bullet exists because description-based skill triggering undertriggers while an `AGENTS.md` line is loaded in every session; the skill is also project-local, so it is invisible outside this repo. Do not hand-write a preset value that `recommend.sh` would have measured — and do overrule the script when a hand measurement contradicts it, recording both numbers. Measuring throughput of an already-configured model belongs to the neighbouring skill `tilloh-local-llm-bench` instead.
- Verification here is cheap in consequence and expensive in time: loading a 16 GB model and reading `nvidia-smi` costs minutes but changes nothing. Spend those minutes instead of asking. Ask first only when verifying means stopping something the user is currently running (a live `llama-server`, for instance).
- Prefer running the tool that reports a number over deriving it by arithmetic. `llama-fit-params` plus a real prompt run beats KV-per-token estimates — the estimates were wrong by enough to matter. A measurement that contradicts your model of the system is the interesting result, not an error to explain away.
- State what you could not verify rather than smoothing over it. Numbers that hold on one machine and were never tested on the other are marked as untested (e.g. whether the mixed-KV collapse also affects Vulkan/lieselotte). An unverified claim presented as fact is worse than an admitted gap, because the next reader has no way to tell.
